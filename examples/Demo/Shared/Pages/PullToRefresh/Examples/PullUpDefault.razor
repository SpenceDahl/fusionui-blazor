<style>
    .pull-demo {
        height: 51.2vh;
        max-width: 400px;
        border: calc(var(--stroke-width)* 1px) solid var(--neutral-stroke-rest);
        border-radius: calc(var(--control-corner-radius)* 1px);
        overflow: auto;
    }
    .pull-content {
        user-select: none;
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        padding: calc(var(--design-unit) * 2px);
    }
</style>

<div class="pull-demo">
    <FluentPullToRefresh Direction="@PullDirection.Up" OnRefreshAsync="OnRefresh" PullTipHeight="32">
        <PullingTemplate>Pull up to refresh</PullingTemplate>
        <LoadingTemplate>
            <FluentProgress Width="150px;"/>
        </LoadingTemplate>
        <ChildContent>
        <div class="pull-content">
            @for (int i = 1; i <= count; i++)
            {
                <span @key="i">item @i</span>
            }
        </div>
        </ChildContent>
    </FluentPullToRefresh>
</div>

@code {
    int refreshcount = 0;
    int count = 20;

    public async Task<bool> OnRefresh()
    {
        refreshcount++;
        if (count < 100)
        {
            await Task.Delay(1000);
            count += 20;
            DemoLogger.WriteLine($"Pull up refresh count {refreshcount}");

            return true;
        }

        return false;
    }
}
